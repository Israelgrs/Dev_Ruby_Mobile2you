<%= form_with(model: registration) do |form| %>
  <% if registration.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(registration.errors.count, "error") %> prohibited this registration from being saved:</h2>

      <ul>
        <% registration.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= collection_select(:registration, :user_id, @usersForSelect, :id, :name)%>
  </div>

  <div class="field">
    <%= form.label :event_id %>
    <%= collection_select(:registration, :event_id, @eventsForSelect, :id, :event_name) %>
  </div>

  <div class="field">
    <%= form.label :registration_date %>
    <%= form.datetime_select :registration_date %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
