<%= form_with(model: award) do |form| %>
  <% if award.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(award.errors.count, "error") %> prohibited this award from being saved:</h2>

      <ul>
        <% award.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :event_id %>
    <%= collection_select(:award, :event_id, @eventsForSelectInAwards, :id, :event_name) %>
  </div>

  <div class="field">
    <%= form.label :award_name %>
    <%= form.text_field :award_name %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= collection_select(:award, :user_id, @usersForSelectInAwards, :id, :email, {:prompt => "Selecione um usuário"}) %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
